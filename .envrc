#!/usr/bin/env bash
# Project environment configuration for reversible-meta-synthesis

export PROJECT_NAME="reversible-meta-synthesis"
export PROJECT_ROOT="$(pwd)"
export PROJECT_TMUX_SESSION="${PROJECT_NAME}-dev"

# Language-specific paths
export CLOJURE_SRC="${PROJECT_ROOT}/src/reversible_meta_synthesis"
export HY_SRC="${PROJECT_ROOT}/src/hy"
export SCHEME_SRC="${PROJECT_ROOT}/src/scheme"
export PROLOG_SRC="${PROJECT_ROOT}/src/prolog"

# Development tools configuration
export EDITOR="emacs"
export EMACS_CONFIG="${PROJECT_ROOT}/${PROJECT_NAME}.el"

# Optional: Add project bin to PATH
PATH_add "${PROJECT_ROOT}/bin"

# Optional: Set up language-specific environment
export GUILE_LOAD_PATH="${PROJECT_ROOT}/src/scheme:${GUILE_LOAD_PATH}"